# Orca

Desktop app for orchestrating multiple Claude Code CLI sessions.

Fresh start — rebuilding from a cleaner design.
The previous implementation (v0) is preserved at branch `archive/v0` / tag `v0-archive`.
